extends Label
class_name ExpressionIcon

## Disabled — character animations now show mood instead.
## Kept as empty script to avoid errors in Player.tscn.

func _ready() -> void:
	visible = false
	text = ""
